{
  "rules": {
    ".read": false,
    ".write": false,

    "students": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid",
        ".validate": "newData.hasChildren()"
      }
    }
  }
}
